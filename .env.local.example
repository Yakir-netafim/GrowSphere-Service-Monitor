# ============================================================
# Netafim Service Monitor — Environment Variables
# Copy this file to .env.local and fill in your values
# ============================================================

# --- Vercel KV (Redis) ---
# These are set automatically when you connect a KV store in the Vercel dashboard
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# --- Email (Resend) ---
# Get your key at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxxxxxx

# Sender address — must be a verified domain in Resend (or use onboarding@resend.dev for testing)
EMAIL_FROM=Netafim Monitor <alerts@yourdomain.com>

# Comma-separated list of recipients
EMAIL_TO=your.email@netafim.com

# --- Microsoft Teams (optional) ---
# Incoming Webhook URL from a Teams channel connector
TEAMS_WEBHOOK_URL=

# --- Cron Security (optional) ---
# Set a secret and pass it as "Authorization: Bearer <CRON_SECRET>" header
# Vercel automatically adds this header for your cron jobs when set in the dashboard
CRON_SECRET=

# --- SSL (local dev only) ---
# If k8s/Azure services have self-signed certs that Node.js rejects, add this for local testing ONLY.
# DO NOT set this on Vercel production.
# NODE_TLS_REJECT_UNAUTHORIZED=0
